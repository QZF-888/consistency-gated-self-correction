# 方法说明

一致性门控自我修正的核心思想是：不要默认相信“让模型再检查一遍”一定有用。直接答案只有在模型自己的采样答案明显不支持它时，才会被替换。

流程：

1. 生成一个确定性的直接答案。
2. 对同一个题目再采样 K 个答案。
3. 计算采样答案中有多少比例与直接答案一致。
4. 如果一致性低于阈值，就用采样多数答案替换直接答案；否则保留直接答案。

这种设计主要针对标准自我修正的常见问题：无条件修正有时会把原本正确的答案改错。
